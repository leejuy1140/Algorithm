/* 스택 & 스위핑 */

#include <stdio.h>
#include <stack>
using namespace std;

const int MAX = 100001;

int main()
{
	scanf("%d", &n);

	int hist[MAX];
	for (int i = 0; i < n; i++)
		scanf("%lld", &hist[i]);



	return 0;
}